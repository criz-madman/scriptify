PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL / Supabase)
DATABASE_URL=postgresql://postgres:password@localhost:5432/scriptify_db

PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=scriptify_db
PGSSL=false

JWT_SECRET=scriptify_super_secret_jwt_key_production_2026_ae
JWT_EXPIRES_IN=7d

# Nodemailer credentials (blank uses console mock for local dev)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="Scriptify Support" <no-reply@scriptify.dev>

# Platform Economics
INITIAL_CREDITS=100
COST_PER_SCRIPT=1

CLIENT_URL=http://localhost:5000,http://127.0.0.1:5000
